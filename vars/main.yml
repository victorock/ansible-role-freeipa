---
freeipa_packages:
  - ipa-server
  - ipa-server-dns

freeipa_services:
  - https
  - http
  - ldaps
  - ldap
  - kerberos
  - kpasswd
  - dns
  - ntp
  - ssh

freeipa_time: "{{ '%Y-%m-%d_%H:%M:%S' | strftime }}"
